<div class="container">
  <% @restaurants.each do |restaurant| %>
  <h1><%= link_to restaurant.name, restaurant_path(restaurant), class: "text-decoration-none" %></h1>
  <% end %>
  <h4><%= link_to "Add a new restaurant", new_restaurant_path, class: "text-decoration-none" %></h4>
</div>
